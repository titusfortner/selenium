language: java

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script:
  - "sh ls -al"
  - "sh which chromedriver"
  - "sh which chromium-browser"
  - "sh which google-chrome"
  - declare -xp
